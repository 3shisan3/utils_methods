用途：
    贝塞尔曲线

说明： 
1. **二次贝塞尔曲线**：
   - 通过3个控制点（起点、控制点、终点）生成曲线。
   - 使用公式 \( B(t) = (1-t)^2 P_0 + 2(1-t)t P_1 + t^2 P_2 \) 计算曲线上的点。

2. **三次贝塞尔曲线**：
   - 通过4个控制点（起点、控制点1、控制点2、终点）生成曲线。
   - 使用公式 \( B(t) = (1-t)^3 P_0 + 3(1-t)^2 t P_1 + 3(1-t) t^2 P_2 + t^3 P_3 \) 计算曲线上的点。
